@model IEnumerable<BloodDonation.Core.Entities.Donation>

@{
    ViewData["Title"] = "إدارة طلبات التبرع";
}

<div class="container mt-5">
    <div class="card shadow">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
            <h3 class="mb-0">📋 طلبات التبرع المعلقة</h3>
            <span class="badge bg-warning text-dark">Total: @Model.Count()</span>
        </div>
        <div class="card-body">
            @if (!Model.Any())
            {
                <div class="alert alert-success text-center">
                    <h5>لا يوجد اي طلبات </h5>
                    <p>كل الطلبات تم التعامل معها.</p>
                </div>
            }
            else
            {
                <table class="table table-striped align-middle">
                    <thead>
                        <tr>
                            <th>المتبرع</th>
                            <th>فصيلة الدم</th>
                            <th>التاريخ</th>
                            <th>ملاحظات</th>
                            <th class="text-center">الإجراء</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <strong>@item.Donor?.FirstName @item.Donor?.LastName</strong><br/>
                                </td>
                                <td>
                                    <span class="badge bg-danger fs-6">@item.Donor?.BloodType</span>
                                </td>
                                <td>@item.DonationDate.ToString("dd/MM/yyyy")</td>
                                <td>@(item.Notes ?? "-")</td>
                                <td class="text-center">
                                    <div class="btn-group" role="group">
                                        <a asp-action="Approve" asp-route-id="@item.Id" class="btn btn-success btn-sm">
                                            ✅ موافقة
                                        </a>
                                        <a asp-action="Reject" asp-route-id="@item.Id" class="btn btn-danger btn-sm">
                                            ❌ رفض
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
        </div>
    </div>
</div>