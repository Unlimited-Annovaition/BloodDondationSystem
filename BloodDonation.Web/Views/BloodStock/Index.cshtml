@model IEnumerable<BloodDonation.Core.Entities.BloodStock>

@{
    ViewData["Title"] = "مخزون الدم";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-danger fw-bold">🩸 لوحة المخزون (Blood Inventory)</h2>
        <span class="badge bg-dark fs-5">Total Types: @Model.Count()</span>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-warning">
            <strong>تنبيه:</strong> المخزون فارغ تماماً! لم يتم تسجيل أي أنواع دم بعد.
        </div>
    }

    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-3 mb-4">
                <div class="card shadow-sm text-center border-0 h-100" 
                     style="background-color: @(item.Quantity < 5 ? "#ffebee" : "#e8f5e9");">
                    
                    <div class="card-body d-flex flex-column justify-content-center">
                        <h1 class="display-4 fw-bold text-danger">
                            @item.BloodType?.Name
                        </h1>
                        
                        <hr class="my-2" />

                        <h4 class="card-title text-dark">
                            الكمية: <span class="fw-bold" style="font-size: 1.5em">@item.Quantity</span>
                        </h4>
                        
                        @if (item.Quantity == 0)
                        {
                            <span class="badge bg-danger mt-2">نفذت الكمية! 🆘</span>
                        }
                        else if (item.Quantity < 5)
                        {
                            <span class="badge bg-warning text-dark mt-2">منخفض ⚠️</span>
                        }
                        else
                        {
                            <span class="badge bg-success mt-2">متوفر ✅</span>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
</div>