@model BloodDonation.Core.Entities.BloodRequest

@{
    ViewData["Title"] = "طلب وحدات دم";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">🏥 طلب تزويد بالدم</h3>
                </div>
                <div class="card-body">
                    <form asp-action="Create">
                        
                        <div class="mb-3">
                            <label class="form-label fw-bold">فصيلة الدم المطلوبة</label>
                            <select asp-for="BloodTypeId" class="form-select" asp-items="ViewBag.BloodTypeId">
                                <option value="">-- اختر الفصيلة --</option>
                            </select>
                            <span asp-validation-for="BloodTypeId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">العدد (وحدات)</label>
                            <input asp-for="Quantity" class="form-control" type="number" min="1" max="100" />
                            <span asp-validation-for="Quantity" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">🚀 إرسال الطلب</button>
                            <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">إلغاء</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>